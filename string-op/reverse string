#include <bits/stdc++.h>
using namespace std;

int main(){
    string a;
    
    cin >> a >>endl;
    for (int i = a.size()-1; i>=0; --i){
        string b.push_back(a[i]);
    }
    cout<< b ;
    
    
}